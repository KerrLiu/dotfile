set $mod Mod4

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock -i "/home/kerr/.config/i3/lock_bg.png"

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
# bindsym $mod+Return exec --no-startup-id i3-sensible-terminal
bindsym $mod+Return exec --no-startup-id st

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec --no-startup-id dmenu_run -nb "#222D31"
# bindsym $mod+r exec --no-startup-id dmenu_run -nb "#222D31"

# start rofi for show run 
# bindsym $mod+r exec --no-startup-id rofi -show run

# change focus
bindsym $mod+j focus left
bindsym $mod+l focus down
bindsym $mod+h focus up
bindsym $mod+k focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left 30
bindsym $mod+Shift+j move down 30
bindsym $mod+Shift+k move up 30
bindsym $mod+Shift+l move right 30

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left 30
bindsym $mod+Shift+Down move down 30
bindsym $mod+Shift+Up move up 30
bindsym $mod+Shift+Right move right 30

# split in horizontal orientation
bindsym $mod+i split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+p layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

bindsym $mod+Shift+u move scratchpad
bindsym $mod+u scratchpad show
# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# move Workspace bindings
bindsym $mod+Tab workspace back_and_forth

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

bindsym Control+Shift+Left resize shrink width 10 px or 10 ppt
bindsym Control+Shift+Down resize grow height 10 px or 10 ppt
bindsym Control+Shift+Up resize shrink height 10 px or 10 ppt
bindsym Control+Shift+Right resize grow width 10 px or 10 ppt

bindsym Control+Shift+h resize shrink width 10 px or 10 ppt
bindsym Control+Shift+j resize grow height 10 px or 10 ppt
bindsym Control+Shift+k resize shrink height 10 px or 10 ppt
bindsym Control+Shift+l resize grow width 10 px or 10 ppt


# Output xrandr
# exec --no-startup-id xrandr --output eDP1 --mode 1368x768 --pos 1920x312 --rotate normal --output DP1 --off --output HDMI1 --off --output HDMI2 --primary --mode 1920x1080 --pos 0x0 --rotate normal --output VIRTUAL1 --off

# Workspace output
workspace $ws1 output HDMI2
workspace $ws7 output eDP1

# Window or titel style
new_window none
new_float none
hide_edge_borders none
gaps inner 14

# Designated application in workspace
assign [class="Google-chrome"] $ws2
assign [class="jetbrains-studio"] $ws3
assign [class="Eclipse"] $ws3
assign [class="Java"] $ws6
assign [class="Qq"] $ws3
assign [class="Epdfview"] $ws6
assign [class="Bookworm"] $ws6
# assign [class="Zathura"] $ws6
assign [class="XMind"] $ws6
assign [class="Typora"] $ws6
assign [class="obs"] $ws7
# assign [class="mpv"] $ws7
# assign [class="mpv"] $ws7


# start Fcitx 
# exec --no-startup-id fcitx

# Start

# exec --no-startup-id ~/Workspace/shell/start.sh

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec --no-startup-id nm-applet

# start feh setting bakground
# exec --no-startup-id feh --randomize --bg-fill --no-fehbg ~/Pictures/Wallpapers/

# start window compton
# exec --no-startup-id compton -b

# start polybar
# exec_always --no-startup-id ~/.config/i3/polybar.sh &

# Screen 
# bindsym --release Print exec --no-startup-id ~/Workspace/shell/screen.sh all
# bindsym --release Control+Print exec --no-startup-id ~/Workspace/shell/screen.sh short
# bindsym --release Control+Mod1+A exec --no-startup-id ~/Workspace/shell/screen.sh select

# Replace wallpaper
# bindsym Control+$mod+m exec --no-startup-id feh --randomize --bg-fill --no-fehbg ~/Pictures/Wallpapers/

# Lock
bindsym Control+$mod+l exec --no-startup-id ~/.config/i3/lock.sh &

# Run obs-studio for optirun
# bindsym $mod+o exec --no-startup-id optirun obs

# Browser start
# bindsym $mod+b exec --no-startup-id google-chrome-stable

# File explorer
# bindsym $mod+e exec --no-startup-id pcmanfm

# Start Ranger for st
# bindsym $mod+Shift+Return exec --no-startup-id st -e ranger ~/

# for sound control  
# bindsym XF86AudioRaiseVolume exec --no-startup-id ~/Workspace/shell/amixerMaster.sh add
# bindsym XF86AudioLowerVolume exec --no-startup-id ~/Workspace/shell/amixerMaster.sh cut
# bindsym XF86AudioMute exec --no-startup-id ~/Workspace/shell/amixerMaster.sh tog

# for backlight
# bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 5
# bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 5

# Media player controls
# bindsym XF86AudioPlay exec --no-startup-id playmusic play
# bindsym XF86AudioPause exec --no-startup-id playmusic stop
# bindsym XF86AudioNext exec --no-startup-id playmusic stop
# bindsym XF86AudioPrev exec --no-startup-id playmusic up
