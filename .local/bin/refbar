#!/bin/bash

# Refresh the dwmbar

# if [ $XDG_SESSION_DESKTOP == i3 ]; then
# 		kill "$(pstree -lp | grep i3bar | sed "s/.*sleep(\([0-9]\+\)).*/\1/")"
# else
# 		kill "$(pstree -lp | grep -- -dwmbar | sed "s/.*sleep(\([0-9]\+\)).*/\1/")"
# fi


[ $XDG_SESSION_DESKTOP == dwm-session ] && kill "$(pstree -lp | grep -- -dwmbar | sed "s/.*sleep(\([0-9]\+\)).*/\1/")"
